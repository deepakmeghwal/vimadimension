# ==============================================
# PRODUCTION ENVIRONMENT CONFIGURATION
# Copy this to .env.production and fill in values
# ==============================================

# ================== DATABASE ==================
MYSQL_HOST=your-rds-endpoint.amazonaws.com
MYSQL_PORT=3306
MYSQL_DATABASE=project_tracker_db
MYSQL_USER=tracker_app_user
MYSQL_PASSWORD=CHANGE_ME_STRONG_PASSWORD

SPRING_DATASOURCE_URL=jdbc:mysql://${MYSQL_HOST}:${MYSQL_PORT}/${MYSQL_DATABASE}?useSSL=true&requireSSL=true&serverTimezone=UTC
SPRING_DATASOURCE_USERNAME=${MYSQL_USER}
SPRING_DATASOURCE_PASSWORD=${MYSQL_PASSWORD}

# ================== EMAIL CONFIGURATION ==================
# Choose ONE of the following options:

# -------- OPTION 1: AWS SES (RECOMMENDED) --------
# Best for production - high deliverability, cheap, no security warnings
# Setup: AWS Console → SES → SMTP Settings → Create SMTP credentials
MAIL_HOST=email-smtp.ap-south-1.amazonaws.com
MAIL_PORT=587
MAIL_USERNAME=your-ses-smtp-username
MAIL_PASSWORD=your-ses-smtp-password
MAIL_FROM=noreply@your-verified-domain.com

# -------- OPTION 2: SendGrid (Free tier: 100 emails/day) --------
# Setup: sendgrid.com → Settings → API Keys
# MAIL_HOST=smtp.sendgrid.net
# MAIL_PORT=587
# MAIL_USERNAME=apikey
# MAIL_PASSWORD=SG.your-sendgrid-api-key
# MAIL_FROM=noreply@your-domain.com

# -------- OPTION 3: Resend (Free tier: 3000 emails/month) --------
# Setup: resend.com → API Keys
# MAIL_HOST=smtp.resend.com
# MAIL_PORT=587
# MAIL_USERNAME=resend
# MAIL_PASSWORD=re_your-resend-api-key
# MAIL_FROM=noreply@your-domain.com

# -------- OPTION 4: Mailgun --------
# MAIL_HOST=smtp.mailgun.org
# MAIL_PORT=587
# MAIL_USERNAME=postmaster@your-domain.mailgun.org
# MAIL_PASSWORD=your-mailgun-password
# MAIL_FROM=noreply@your-domain.com

# ================== APPLICATION URLs ==================
# Frontend URL (for email links)
APP_FRONTEND_URL=https://your-domain.com
APP_NAME=YourAppName

# Backend API URL
API_BASE_URL=https://api.your-domain.com

# ================== SECURITY ==================
# JWT Secret (generate with: openssl rand -base64 64)
JWT_SECRET=CHANGE_ME_GENERATE_STRONG_SECRET_KEY

# CORS allowed origins (comma-separated)
CORS_ALLOWED_ORIGINS=https://your-domain.com,https://www.your-domain.com

# ================== SPRING PROFILES ==================
SPRING_PROFILES_ACTIVE=prod

# ================== LOGGING ==================
LOGGING_LEVEL_ROOT=INFO
LOGGING_LEVEL_ORG_EXAMPLE=INFO

